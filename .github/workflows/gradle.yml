# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: nickname-api build

on:
  push:
    branches: [ master, feature-* ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: 'adopt'

      - name: Setup artifactory properties
        shell: bash
        env:
          ARTIFACTORY_CONTEXTURL: ${{ secrets.ARTIFACTORY_CONTEXTURL }}
          ARTIFACTORY_USER: ${{ secrets.ARTIFACTORY_USER }}
          ARTIFACTORY_PASSWORD: ${{ secrets.ARTIFACTORY_PASSWORD }}
        run: |
          mkdir -p ~/.gradle
          echo -e artifactory_contextUrl=$ARTIFACTORY_CONTEXTURL > ~/.gradle/gradle.properties
          echo -e artifactory_user=$ARTIFACTORY_USER >> ~/.gradle/gradle.properties
          echo -e artifactory_password=$ARTIFACTORY_PASSWORD >> ~/.gradle/gradle.properties

      - name: Build with Gradle
        run: ./gradlew clean build

      - name: Publish to artifactory
        run: ./gradlew artifactoryPublish

  artifactory:
    runs-on: ubuntu-latest
    steps:
      - name: Setup JFrog cli
        uses: jfrog/setup-jfrog-cli@v2

      - name: Ping and Download artifact
        env:
          ARTIFACTORY_CONTEXTURL: ${{ secrets.ARTIFACTORY_CONTEXTURL }}
          ARTIFACTORY_USER: ${{ secrets.ARTIFACTORY_USER }}
          ARTIFACTORY_PASSWORD: ${{ secrets.ARTIFACTORY_PASSWORD }}
        run: |
          jf config add --user=$ARTIFACTORY_USER --access-token=$ARTIFACTORY_PASSWORD --url=$ARTIFACTORY_CONTEXTURL
          jf rt ping
          jf rt dl *.jar --sort-by=created --sort-order=desc --limit=1

